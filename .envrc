# Credentials are populated in the ignored private companion.
if command -v source_env_if_exists >/dev/null 2>&1; then
  source_env_if_exists .envrc.private
else
  test ! -f .envrc.private || . ./.envrc.private
fi
export AWS_ACCESS_KEY_ID="${COLORS_PAR_AWS_ACCESS_KEY_ID:-}"
export AWS_SECRET_ACCESS_KEY="${COLORS_PAR_AWS_SECRET_ACCESS_KEY:-}"
export AWS_DEFAULT_REGION="${AWS_DEFAULT_REGION:-us-east-1}"
